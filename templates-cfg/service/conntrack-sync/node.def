help: Enable connection syncing service

priority: 995 # testing right now

end: if [ ${COMMIT_ACTION} = 'DELETE' ]; then
        echo execute conntrackd disable action
        sudo /opt/vyatta/sbin/vyatta-conntrack-sync.pl --action=disable
     else
        echo execute conntrackd enable action
        sudo /opt/vyatta/sbin/vyatta-conntrack-sync.pl --action=enable
     fi;
