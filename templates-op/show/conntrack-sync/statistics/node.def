help: Show connection syncing statistics

run: if [ -d $VYATTA_ACTIVE_CONFIGURATION_DIR/service/conntrack-sync ] ; then
       sudo /opt/vyatta/bin/sudo-users/vyatta-op-conntrack-sync.pl \
         --action=show-statistics
     else
       echo conntrack-sync not configured
     fi
